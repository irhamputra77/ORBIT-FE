API_BASE_URL=
NEXT_PUBLIC_API_BASE_URL=
# Optional: point this to a DNS-only/unproxied upload hostname to avoid CDN
# read timeouts while the backend parses large documents.
NEXT_PUBLIC_UPLOAD_API_BASE_URL=
NEXT_PUBLIC_ENABLE_ENGINEERING_DASHBOARD=false
NEXT_PUBLIC_ENABLE_EES_APPROVAL=false
